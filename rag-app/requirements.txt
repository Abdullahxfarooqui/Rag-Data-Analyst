# Streamlit UI
streamlit>=1.28.0

# Data handling (CSV only - NO pandas for Excel to avoid segfaults)
pandas>=2.0.0

# Excel reading - PURE PYTHON ONLY (no C extensions)
openpyxl>=3.1.0  # For .xlsx files (pure Python)
xlrd==1.2.0  # For .xls files (last pure Python version)

# Visualizations
plotly>=5.18.0

# HTTP requests (for API calls)
requests>=2.31.0

# NumPy for vector operations (NOT used in Excel extraction)
numpy>=1.24.0
